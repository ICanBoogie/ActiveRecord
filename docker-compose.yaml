---
services:
  app84:
    build:
      context: .
      args:
        PHP_VERSION: "8.4"
    environment:
      PHP_IDE_CONFIG: 'serverName=icanboogie-activerecord'
    volumes:
      - .:/app:delegated
      - ~/.composer:/root/.composer:delegated
    working_dir: /app
